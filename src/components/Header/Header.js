import React from 'react';
import './styles.css';

const Header = () => {
    return (
        <header className="header">
            <h1>Productivity To Do List</h1>
        </header>
    );
};

export default Header;
